name: PR Quality Gates (Legacy)

on:
  workflow_dispatch:

jobs:
  legacy-note:
    name: Legacy Workflow Notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflows
        run: |
          echo "This legacy workflow has been replaced by:"
          echo "  - pr-check.yml"
          echo "  - branch-name.yml"
          echo "  - pr-title.yml"
          echo "  - security.yml"
